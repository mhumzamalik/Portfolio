export const AI_CONFIG = {
  // Production-ready Google Gemini model supported in the current environment
  model: "gemini-3.5-flash",
  
  // API version to use
  apiVersion: "v1beta",
  
  // Default temperature configuration for portfolio assistant grounding
  defaultTemperature: 0.3,
  
  // Maximum response token length to keep it concise and fast
  maxOutputTokens: 400,
};
